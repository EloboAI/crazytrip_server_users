# CrazyTrip Server Users - Variables de Entorno (Ejemplo)
# Copia este archivo como .env y ajusta los valores según tu entorno

HOST=0.0.0.0
PORT=8080
DATABASE_URL=postgresql://postgres:password@localhost:5432/crazytrip_users
SERVER_ENVIRONMENT=development # development/production
JWT_SECRET=supersecretkey
# Opcional: SMTP para emails
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=youruser
SMTP_PASS=yourpass
